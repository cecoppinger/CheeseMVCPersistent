@model List<CheeseMVC.Models.CheeseCategory>
@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<ul>
    @foreach (var item in Model)
    {
        <li>@item.Name</li>
    }
</ul>

<a style="display: block;" asp-controller="Category" asp-action="Add">Add Category</a>
